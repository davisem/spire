machine:
  pre:
    - sudo apt-get install build-essentials
  
  python:
    version: 2.7.10


dependencies:
  override:
    - echo "Trying to override this"

test:
  override:
    nosetests -s tests/kmer_tests.py